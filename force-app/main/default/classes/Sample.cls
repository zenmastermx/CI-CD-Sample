public with sharing class Sample {
    public static string getBackwardString(String str) {
        String myStr = 'asdf';

        if(str == 'abc') {
            myStr = 'Hello';
        }
        return myStr;
    }
}
